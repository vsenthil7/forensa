[tool.poetry]
name = "forensa"
version = "0.1.0"
description = "Cryptographic evidence layer for enterprise AI agents"
authors = ["v_sen <v_sen@forensa.dev>"]
license = "MIT"
readme = "README.md"
packages = [
    { include = "apps" },
    { include = "packages" },
]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "0.115.4"
uvicorn = { extras = ["standard"], version = "0.32.0" }
pydantic = "2.9.2"
pydantic-settings = "2.6.1"
sqlalchemy = { extras = ["asyncio"], version = "2.0.36" }
asyncpg = "0.30.0"
alembic = "1.13.3"
cryptography = "43.0.3"
httpx = "0.27.2"
python-multipart = "0.0.17"
orjson = "3.10.11"
structlog = "24.4.0"
opentelemetry-api = "1.28.1"
opentelemetry-sdk = "1.28.1"
opentelemetry-instrumentation-fastapi = "0.49b1"

[tool.poetry.group.dev.dependencies]
pytest = "8.3.3"
pytest-asyncio = "0.24.0"
pytest-cov = "5.0.0"
hypothesis = "6.118.0"
ruff = "0.7.2"
mypy = "1.13.0"
asgi-lifespan = "2.1.0"

[tool.poetry.scripts]
forensa-api = "apps.api.main:run"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = ["-ra", "--strict-markers", "--strict-config", "--cov=apps", "--cov=packages", "--cov-branch", "--cov-report=term-missing", "--cov-fail-under=100"]
norecursedirs = ["_backup", ".git", ".venv", "__pycache__", "node_modules", "alembic"]
python_files = ["test_*.py"]
filterwarnings = ["error"]

[tool.coverage.run]
branch = true
source = ["apps", "packages"]
omit = ["*/__init__.py", "*/tests/*", "alembic/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
show_missing = true
fail_under = 100

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
