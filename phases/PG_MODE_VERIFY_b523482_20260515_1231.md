# PG-mode verification - HEAD b523482

**Stamp:** 2026-05-15 12:31 +01:00
**HEAD:** b523482 ([FEAT] CP9.31 scripts/seed_demo_data.py - dual-sign seeded receipts)
**Mode:** PG-mode (FORENSA_TEST_DB_URL=postgresql+asyncpg://forensa:forensa@localhost:5433/forensa)
**Docker container:** forensa-pg (Up 17 hours, port 5433->5432)
**Author:** Claude

## Result

```
947 passed in 25.30s
```

Where 947 = 921 default-mode tests + 26 PG-only integration tests.

The 26 PG-only tests exercise things SQLite cannot model:

- `tests/integration/test_pg_migrations.py` (7 tests) - alembic upgrade head from an empty schema; downgrade chain reversibility
- `tests/integration/test_pg_partial_unique_active.py` (5 tests) - the `uq_policy_bundles_one_active_per_tenant` partial UNIQUE index that prevents two active bundles per tenant
- `tests/integration/test_pg_append_only_triggers.py` (6 tests) - DB-level triggers refusing UPDATE/DELETE on `receipts` and `events` tables
- `tests/integration/test_pg_anchor.py` (8 tests) - end-to-end anchor lifecycle including TSA mock + tombstone rotation

## What this verifies

1. **CP9.28 BR-13 M&A export** does not break the underlying ledger. The `ma_export` module reads receipts via the same `get_receipts_for_tenant` path the other routes use; PG-mode confirms FK + index assumptions hold.
2. **CP9.29 BR-12 tabletop** does not write to any table (read-only on bundle lookup); PG-mode confirms `get_bundle_by_id` works against real Postgres.
3. **CP9.30 seed-data + CP9.31 dual-signature** wire the production code paths (`anchor_day`, `write_bundle`, the approval workflow, `build_receipt`) but only call them in the seed script itself - not in the runtime suite. PG-mode confirms no other code path was perturbed.
4. **CP9.28-CP9.31 documentation** (BRD truth-up, README badges) cannot move test count by construction.

## What this does NOT verify

- The seed script itself running end-to-end against PG. Tracked as `NEW-P10.X.seed-demo-data-pg-integration-test` - a future `tests/integration/test_seed_demo_data_pg.py` would invoke `seed_demo_data.seed()` against a clean DB and assert the resulting row counts + `receipt.agent_signature IS NOT NULL` on every row.
- The `openssl ts -verify` step in `tools/demo.sh` against MockTimestampClient TSR bytes. Tracked as `NEW-P11.X.real-tsa-in-seed-script`.

## Closure

IP #3 (PG-mode re-verification at HEAD b523482) - CLOSED.

The submission-week artefact set + the 10/13 BR scoreboard + the dual-signed seed data + the badged README all coexist at HEAD b523482 without regressing the PG layer. Judge-ready.
